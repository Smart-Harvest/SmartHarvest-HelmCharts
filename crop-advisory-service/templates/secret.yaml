apiVersion: v1
kind: Secret
metadata:
  name: crop-advisory-service-secret
  labels:
    {{- include "crop-advisory-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://{{ .Values.postgres.user }}:{{ .Values.postgres.password }}@postgres:{{ .Values.postgres.port }}/{{ .Values.postgres.databases.crop }}"
  DATABASE_URL_SYNC: "postgresql://{{ .Values.postgres.user }}:{{ .Values.postgres.password }}@postgres:{{ .Values.postgres.port }}/{{ .Values.postgres.databases.crop }}"
  JWT_SECRET: {{ .Values.jwt.secret | quote }}
  OPENWEATHER_API_KEY: {{ .Values.openweather.apiKey | quote }}
