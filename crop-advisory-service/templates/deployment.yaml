apiVersion: apps/v1
kind: Deployment
metadata:
  name: crop-advisory-service
  labels:
    {{- include "crop-advisory-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.cropAdvisoryService.replicas }}
  selector:
    matchLabels:
      {{- include "crop-advisory-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "crop-advisory-service.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: crop-advisory-service
          image: "{{ .Values.cropAdvisoryService.image }}:{{ .Values.cropAdvisoryService.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.cropAdvisoryService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: crop-advisory-service-config
            - secretRef:
                name: crop-advisory-service-secret
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 40
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
