apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketplace-service
  labels:
    {{- include "marketplace-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.marketplaceService.replicas }}
  selector:
    matchLabels:
      {{- include "marketplace-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "marketplace-service.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: marketplace-service
          image: "{{ .Values.marketplaceService.image }}:{{ .Values.marketplaceService.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.marketplaceService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: marketplace-service-config
            - secretRef:
                name: marketplace-service-secret
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 40
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
