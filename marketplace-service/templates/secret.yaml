apiVersion: v1
kind: Secret
metadata:
  name: marketplace-service-secret
  labels:
    {{- include "marketplace-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://{{ .Values.postgres.user }}:{{ .Values.postgres.password }}@postgres:{{ .Values.postgres.port }}/{{ .Values.postgres.databases.market }}"
  DATABASE_URL_SYNC: "postgresql://{{ .Values.postgres.user }}:{{ .Values.postgres.password }}@postgres:{{ .Values.postgres.port }}/{{ .Values.postgres.databases.market }}"
  JWT_SECRET: {{ .Values.jwt.secret | quote }}
