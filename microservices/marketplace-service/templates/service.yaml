{{- $svc := index .Values.services "marketplace-service" -}}
apiVersion: v1
kind: Service
metadata:
  name: marketplace-service
  labels:
    {{- include "marketplace-service.labels" . | nindent 4 }}
spec:
  type: {{ $svc.service.type | default .Values.global.service.type }}
  ports:
    - port: {{ $svc.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marketplace-service.selectorLabels" . | nindent 4 }}
