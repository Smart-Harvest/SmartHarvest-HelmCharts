apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ .Values.infrastructure.gateway.className }}
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
spec:
  controllerName: {{ .Values.infrastructure.gateway.controllerName }}
