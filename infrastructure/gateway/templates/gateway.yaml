# Gateway — dynamically deploys to whatever namespace ArgoCD targets (dev or prod)
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: smartharvest-gateway
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
    environment: {{ .Release.Namespace }}
spec:
  gatewayClassName: {{ .Values.infrastructure.gateway.className }}
  listeners:
    {{- range .Values.infrastructure.gateway.listeners }}
    - name: {{ .name }}
      protocol: {{ .protocol }}
      port: {{ .port }}
      allowedRoutes:
        namespaces:
          from: Same
    {{- end }}